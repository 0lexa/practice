https://www.codewars.com/kata/525821ce8e7b0d240b002615

//JavaScript

const camelize = str =>
  str.toLowerCase().split(/_|\W/g).reduce((pre, val) => pre + (val && (val[0].toUpperCase() + val.slice(1))), ``);
