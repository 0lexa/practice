https://www.codewars.com/kata/58881b859ab1e053240000cc

//JavaScript

const electionsWinners = (votes, k) =>
  (max => (!k * (votes.filter(val => val === max).length === 1)) || votes.filter(val => val + k > max).length)
  (Math.max(...votes));
