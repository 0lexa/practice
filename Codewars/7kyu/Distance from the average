https://www.codewars.com/kata/568ff914fc7a40a18500005c

//JavaScript

const distancesFromAverage = arr =>
  (avg => arr.map(val => Math.round((avg - val) * 100) / 100))
  (arr.reduce((pre, val) => pre + val) / arr.length);
