https://www.codewars.com/kata/605f5d33f38ca800322cb18f

//JavaScript

const tapCodeTranslation = s =>
  (str => [...s.replace(/k/g, `c`)].map(val => `${`.`.repeat((str.indexOf(val) / 5 ^ 0) + 1)} ${`.`.repeat(str.indexOf(val) % 5 + 1)}`).join(` `))
  (`abcdefghijlmnopqrstuvwxyz`);
