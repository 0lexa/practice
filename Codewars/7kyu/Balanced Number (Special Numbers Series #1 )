https://www.codewars.com/kata/5a4e3782880385ba68000018

//JavaScript

const balancedNum = number =>
  ([...`${number}`.slice(`${number}`.length / 2 + 1)].reduce((pre, val, idx) => pre + +val - `${number}`[idx], 0) ? `Not ` : ``) + `Balanced`;
