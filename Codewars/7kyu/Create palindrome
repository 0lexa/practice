https://www.codewars.com/kata/5b7bd90ef643c4df7400015d

//JavaScript

const solve = s =>
  [...s.slice(0, s.length / 2)].map((val, idx) => val.charCodeAt() - s[s.length - idx - 1].charCodeAt()).every(val => !val || Math.abs(val) === 2);
