https://www.codewars.com/kata/569b5cec755dd3534d00000f

//JavaScript

const newAvg = (arr, newavg) =>
  (val => val > 0 ? Math.ceil(val) : function () {throw new Error();}())
  (newavg * (arr.length + 1) - arr.reduce((pre, val) => pre + val, 0));
