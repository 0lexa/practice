https://www.codewars.com/kata/59de795c289ef9197f000c48

//JavaScript

const removeBMW = str => {
  if (typeof str === `string`) return str.replace(/[bwm]/gi, ``);
  throw new TypeError (`This program only works for text.`)
};
