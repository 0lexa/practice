https://www.codewars.com/kata/56dd927e4c9055f8470013a5

//JavaScript

const testit = (act, s) =>
  s.replace(/./g, (val, idx) => act[idx] === `run` ? val === `|` ? `/` : val : val === `|` ? val : `x`);
