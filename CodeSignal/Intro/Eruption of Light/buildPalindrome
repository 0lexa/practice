https://app.codesignal.com/arcade/intro/level-10/ppZ9zSufpjyzAsSEx

//JavaScript

const buildPalindrome = st => {
  for (let i = 0; i < st.length; i++) {
    let pal = st + [...st.slice(0, i)].reverse().join(``);
    if (pal === [...pal].reverse().join(``)) return pal;
  }
};
