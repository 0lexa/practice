https://app.codesignal.com/arcade/intro/level-2/xskq4ZxLyqQMCLshr

//JavaScript

const matrixElementsSum = matrix =>
  matrix.reduce((pre, val, idx) => pre + val.reduce((pre, val, i) => pre + val * matrix.slice(0, idx + 1).every(v => v[i]), 0), 0);
