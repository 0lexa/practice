https://app.codesignal.com/arcade/code-arcade/well-of-integration/ZCD7NQnED724bJtjN

//JavaScript

const addBorder = picture =>
  (val => [val, ...picture.map(val => `*${val}*`), val])
  (`*`.repeat(picture[0].length + 2));
