https://app.codesignal.com/arcade/code-arcade/well-of-integration/kvGfZZxGyjNbD7yxv

//JavaScript

const integerToStringOfFixedWidth = (number, width) =>
  `${number}`.padStart(width, `0`).slice(-width);
