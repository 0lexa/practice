https://app.codesignal.com/arcade/code-arcade/spring-of-integration/MQg7s3dKrP4caN42A

//JavaScript

const arrayPreviousLess = items =>
  items.map((val, idx) => items.slice(0, idx).reverse().find(v => v < val) || -1);
