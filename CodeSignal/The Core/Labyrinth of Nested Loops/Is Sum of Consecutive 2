https://app.codesignal.com/arcade/code-arcade/labyrinth-of-nested-loops/EQSjA5PRfyHueeNkj

//JavaScript

const isSumOfConsecutive2 = n =>
  Array.from(Array(n / 2 ^ 0), (_, idx) => 3 + idx * 2).reduce((pre, val) => pre + !(n % val), 0);
