https://app.codesignal.com/arcade/code-arcade/labyrinth-of-nested-loops/yBFfNv5fTqhcacZ7w

//JavaScript

const isPower = n =>
  [...Array((n ** 0.5 ^ 0) - 1).keys()].some(val => Number.isInteger(+(Math.log(n) / Math.log(val + 2)).toFixed(5)))
    ? true : n === 1;
