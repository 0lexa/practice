https://leetcode.com/problems/duplicate-zeros/

//JavaScript

const duplicateZeros = arr => {
  for (let i = 0; i < arr.length; i++) {
    if (!arr[i]) {
      arr.splice(i++, 0, 0);
      arr.pop();
    }
  }
};
